<nav class="navbar navbar-light bg-light">
    <a class="navbar-brand" href="{{ route('dashboard') }}">Brand</a>
    <form class="form-inline">
        <a href="{{route('account')}}" style="margin-right: 2rem">Account</a>
        <a href="{{route('logout')}}">Log Out</a>
    </form>
</nav>
